- pipeline:
    name: check
    description: |
      Check new commits at zuul
    manager: independent
    trigger:
      qinqon:
        event: ref-updated
    success:
      mysql:
    failure:
      mysql:
